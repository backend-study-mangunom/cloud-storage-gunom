package com.example.myboxservice.global.enums

enum class ExceptionMessage(val message: String) {
    USER_DUPLICATE_EXCEPTION("User is already exist"),
    INTERNAL_SERVER_ERROR("Internal server error")
}